{% extends "base.html.twig" %}
{% block body %}
<h3>Créer une question :</h3>
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
	{{ form_row(form.content) }}

	<input type="submit" class="btn btn-primary" value="Créer une question"/>
	{{ form_end(form) }}
{% endblock %}